{{navbar-footer/team-member team=team}}

{{#if team.isEditable}}
  {{#if team.isNotFull}}
    <btn class="col-md-2 btn btn-default pokemon-button add-pokemon-button" {{action 'addPokemon'}}>Add Pokemon</btn>
  {{/if}}
{{/if}}
