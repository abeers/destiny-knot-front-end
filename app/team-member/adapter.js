import ApplicationAdapter from 'destiny-knot/application/adapter';

export default ApplicationAdapter.extend({
  // updateRecord (store, type, record) {
  //   let api = this.get('host');
  //   let listId = record.list_id;
  //   let url = `${api}/teams/${team_id}/pokemons/${pokemon_id}`;
  //
  //   let data = { team_member: serialized };
  //
  //   return this.ajax(url, 'POST', { data });
  // }
});
